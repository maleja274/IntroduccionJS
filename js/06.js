//Objeto Math

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5);
resultado = Math.ceil(2.5); // redondea hacia arriba
resultado = Math.floor(2.9); // redondea hacia abajo
resultado = Math.sqrt(144); // raiz cuadrada
resultado = Math.abs(-100); //convierte un numero negativo a positivo
resultado = Math.min(2, 5, 7, 9, 6); // toma el numero menor
resultado = Math.max(2, 5, 7, 9, 6); // toma el numero mayor
resultado = Math.random(); // numero aleatorio
resultado = Math.floor(Math.random() * 30); 


console.log(resultado);